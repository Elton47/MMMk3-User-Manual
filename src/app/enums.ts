export enum ModeTypes {
  CLIP = 'CLIP',
  SCENE = 'SCENE'
}

export enum ActionType {
  TOGGLE,
  TOGGLE_SMALL,
  TOUCH_STRIP,
  JOG_WHEEL_ROTATE,
  JOG_WHEEL_PUSH,
  PAD,
  SEPARATOR_SLASH,
  SEPARATOR_PLUS,
  PLAY,
  REC,
  STOP,
  SHIFT,
  MASCHINE,
  FAVOURITE,
  BROWSER
}
